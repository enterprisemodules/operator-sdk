---
- version: v1alpha1
  group: app.example.com
  kind: Database
  playbook: /puppet/playbook.yaml
  finalizer:
    name: finalizer.app.example.com
    role: puppet/role
    vars:
      sentinel: finalizer_running
